0.  Make sure you're using the correct ini file for the API connection

1.  Change Work Package Phase type to Activity in OpenProject instance

Admin->Work packages->Types

The MPXJ python package uses the JPype Java bridge to allow direct access from Python to the MPXJ library. 
JPype is a Python module to provide full access to Java from within Python and requires a working JDK.

